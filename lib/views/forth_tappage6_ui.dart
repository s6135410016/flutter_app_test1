import 'dart:async';

import 'package:flutter/material.dart';

class ForthTappage6UI extends StatelessWidget {
  const ForthTappage6UI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'อุ๊ ต๊ะ',
          style: TextStyle(
            fontFamily: 'Kanit',
            color: Colors.pink,
            fontSize: 50.0,
            fontWeight: FontWeight.bold,
          ),
        ),
      ),
    );
  }
}
